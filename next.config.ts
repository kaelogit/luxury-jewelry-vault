import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // 1. PERFORMANCE: Cache optimized images for 1 hour (3600 seconds) 
    // to stop the "slow loading" on repeat visits.
    minimumCacheTTL: 3600,
    
    // 2. QUALITY: High-res breakpoints for premium displays
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
    
    // 3. FORMATS: Force modern, small file sizes
    formats: ['image/avif', 'image/webp'], 

    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
        port: '',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: 'via.placeholder.com',
        port: '',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: 'ysejihzmlshvelsowztc.supabase.co',
        port: '',
        pathname: '/storage/v1/object/public/**',
      },
      {
        protocol: 'https',
        hostname: '*.supabase.co',
        port: '',
        pathname: '/**',
      },
    ],
  },
  
  // 4. SECURITY: Standard headers for luxury commerce
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          { key: 'X-Frame-Options', value: 'DENY' },
          { key: 'X-Content-Type-Options', value: 'nosniff' },
          { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
        ],
      },
    ];
  },
};

export default nextConfig;